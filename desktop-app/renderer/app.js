// The production BrowserWindow loads the local execution worker UI. This entry is kept as a renderer smoke target for packaging checks.
export const desktopRenderer = { mode: 'execute', auth: 'account-gated' }
